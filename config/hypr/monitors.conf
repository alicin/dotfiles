# Generic monitor configuration - fallback for machines without specific config
# Machine-specific monitor configs are in $HOSTNAME.conf files

# Default monitor setup (will be overridden by machine-specific configs)
monitor=,preferred,auto,auto

# Default workspace setup
workspace=1,default:true
