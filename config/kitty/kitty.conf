include conf.d/behavior.conf
include conf.d/fonts.conf
include conf.d/appearance.conf
include current-theme.conf
# Touchscreen flick scrolling (kitty handles wl_touch natively; the default
# 1.0 multiplier feels dead on the tablet's 120Hz panel).
touch_scroll_multiplier 3.0
