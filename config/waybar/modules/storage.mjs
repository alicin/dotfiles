#! /usr/bin/env zx
$.verbose = false;

// let nvme = await nothrow($`df -Ph | grep nvme`);
// let sda = await nothrow($`df -Ph | grep sda`);

console.log(JSON.stringify({ text: "yellow ", class: "on" }));
