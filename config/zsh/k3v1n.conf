# # K3V1N Tablet - Machine-specific ZSH configuration

# # Tablet-specific aliases
# alias rotate-screen='hyprctl keyword monitor "eDP-1,transform,1"'
# alias rotate-reset='hyprctl keyword monitor "eDP-1,transform,0"'
# alias keyboard='onboard &'
# alias keyboard-off='killall onboard'
# alias tablet-mode='hyprctl keyword general:gaps_in 12 && hyprctl keyword general:gaps_out 16'
# alias laptop-mode='hyprctl keyword general:gaps_in 5 && hyprctl keyword general:gaps_out 8'

# # Touch-friendly aliases (shorter output for small screen)
# alias ll='ls -la --color=auto | head -15'
# alias ps-short='ps aux | head -20'
# alias df-short='df -h | head -10'

# # Power management aliases
# alias power-save='auto-cpufreq --set=powersave'
# alias power-performance='auto-cpufreq --set=performance'
# alias brightness-low='brightnessctl set 20%'
# alias brightness-high='brightnessctl set 80%'

# # Tablet-specific variables
# export TABLET_MODE=1
# export ONBOARD_ENABLED=1
# export TOUCH_DEVICE=1

# # Tablet-specific functions
# function battery-info() {
#     local battery_level=$(upower -i /org/freedesktop/UPower/devices/BAT0 | grep percentage | awk '{print $2}')
#     local battery_status=$(upower -i /org/freedesktop/UPower/devices/BAT0 | grep state | awk '{print $2}')
#     echo "Battery: $battery_level ($battery_status)"
# }

# function auto-rotate() {
#     echo "Starting auto-rotation service..."
#     iio-sensor-proxy &
# }

# function tablet-setup() {
#     echo "Setting up tablet mode..."
#     tablet-mode
#     keyboard
#     brightness-low
#     echo "Tablet mode activated!"
# }

# function laptop-setup() {
#     echo "Setting up laptop mode..."
#     laptop-mode
#     keyboard-off
#     brightness-high
#     rotate-reset
#     echo "Laptop mode activated!"
# }

# function system-info() {
#     echo "=== K3V1N Tablet System Info ==="
#     echo "Hostname: $(hostname)"
#     echo "Uptime: $(uptime -p)"
#     battery-info
#     echo "Temperature: $(sensors | grep 'Core 0' | awk '{print $3}' || echo 'N/A')"
#     echo "Memory: $(free -h | grep Mem | awk '{print $3 "/" $2}')"
# }