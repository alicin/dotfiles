# h4l9000 — AUR packages. yay is bootstrapped by scripts/prepare.sh.
#
# Blank lines and `#` comments are stripped by read_package_list().
#
# quickshell provides `qs` — the bar, launcher, command palette, OSD and
# Control Center that config/hypr/lua/apps.lua drives. It now comes from
# extra/ (see pacman.txt), not the AUR: h4l9000 moved off quickshell-git
# 0.3.0.r11 to extra/quickshell 0.3.0 on 2026-08-18, matching k3v1n. The two
# builds ship identical QML module sets (Bluetooth and Networking included);
# the only files the -git build added were /usr/src/debug sources. qshell
# restarts clean on the release build with every IPC target registered.
#
# rose-pine-hyprcursor satisfies HYPRCURSOR_THEME in config/uwsm/env; the
# XCURSOR_THEME=BreezeX-RosePine-Linux value comes from rose-pine-cursor.
#
# The HyprPanel era (ags-hyprpanel-git / aylurs-gtk-shell-git / libastal-*)
# was uninstalled on 2026-08-18 — qshell had fully replaced it and nothing
# loaded astal any more. caelestia-shell went with it; quickshell-git was
# marked explicit first so the orphan sweep could not take the live desktop.

android-studio
appimagelauncher
appmenu-glib-translator-git
asusctl
bindfs
claude-desktop-bin
curseforge
faugus-launcher
google-chrome
libcava
libpamac-aur
looking-glass
looking-glass-module-dkms
openhue-cli
pamac-aur
python-click-default-group
python-gpustat
python-inquirerpy
python-openapi-pydantic
python-textual-autocomplete
rog-control-center
rose-pine-cursor
rose-pine-gtk-theme-full
rose-pine-hyprcursor
slack-desktop-wayland
stormlib
supergfxctl
systemd-manager-tui
virtio-win
visual-studio-code-bin
yay
