# Global JS tooling. Installed with `pnpm add -g` (see profile-install.sh).
#
# Names only, no versions. genpkg writes this via dump_npm_names(); an earlier
# parse (`awk '{print $1}'` / `awk -F@`) wrote box-drawing glyphs and a stray
# count instead of names, because scoped packages carry their own '@'.

clawhub
firebase-tools
@github/copilot
@google/gemini-cli
mcporter
pm2
serve
@steipete/oracle
supabase
yarn
