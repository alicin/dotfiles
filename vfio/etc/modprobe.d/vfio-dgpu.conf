# CONDITIONAL — only if the Blackwell dGPU fails to resume from D3cold at VM start
# ("works once per boot" reset quirk). Uncomment, then: sudo mkinitcpio -P && reboot.
#options vfio-pci disable_idle_d3=1
